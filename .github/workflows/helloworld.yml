name: "hello world workflow"

# controls when the action will run. triggers the workflow on push to done to master or production branch.
on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print Hello World"
        run: echo "Hello World!"
